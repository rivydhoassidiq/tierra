import 'package:flutter/material.dart';

class Komen extends StatelessWidget {
  const Komen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Komen')),
    );
  }
}
